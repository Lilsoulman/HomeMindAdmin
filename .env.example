# Production: API's public origin, for example https://api.example.com
VUE_APP_API_BASE_URL=http://localhost:5280

# Local development only. Keep API requests same-origin through Vue CLI's proxy.
VUE_APP_API_PROXY_TARGET=http://localhost:5280
